using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using NUnit.Framework;

public class PlayerDamageTest
{
    [Test]
    public void TestDamage()
    {
        /*//ARRANGE
        PlayerMovement damagePlayer = new PlayerMovement();
        float dmg = 20.0f;
        float hitpoints = 100.0f;
        var expectedValue = hitpoints - dmg;
        //ACT
        float finalHitpoints = damagePlayer.TakeDamage(dmg);
        //ASSERT
        Assert.That(finalHitpoints, Is.EqualTo(expectedValue));
        */
    }

}
