using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using NUnit.Framework;

public class HealDamageTest
{
    [Test]
    public void HealDamage_Tests()
    {
        /*//ARRANGE
        PlayerMovement healDmg = new PlayerMovement();
        var hitpoint = 40;
        var heal = 10;
        var expectedValue = hitpoint += heal;

        //ACT
        float testValue = healDmg.HealDamage(heal);

        //ASSERT
        Assert.That(testValue, Is.EqualTo(expectedValue));
        */
    }
}
