using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using NUnit.Framework;

public class PlayerAttackTest
{
    [Test]
    public void PlayerAttack_Test()
    {
        /*
        //Arrange
        PlayerMovement playerAttackMove = new PlayerMovement();
        char userKey = 'j';
        char expectedKey = 'j';

        //ACT
        char testKey = playerAttackMove.Attack(userKey);

        //ASSERT
        Assert.That(testKey, Is.EqualTo(expectedKey));
        */
    }
}

